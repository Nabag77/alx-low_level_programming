#include "main.h"

/**
 *  _abs - function that prints the absolute value of an integer
 *
 * @int: the integer
 *
 * Return: return the integer
 */

int _abs(int)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
